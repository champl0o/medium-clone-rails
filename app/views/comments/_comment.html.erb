<div class="card">
    <div class="card-header">
        <span class="comment-name"><%= comment.user.email %></span>
        <span class="comment-date"><%= comment.created_at.strftime("%Y-%m-%d") %></span>
    </div>
    <div class="card-body">
        <p><%= comment.body %></p>
    </div>
</div>
<br>